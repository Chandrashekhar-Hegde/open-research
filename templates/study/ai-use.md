# AI assistance disclosure

[fill: tools/models and versions actually available, dates, tasks, prompt or
session reference where shareable, human verification, known limitations.
Record no AI use if none. Redact secrets and private data from any shared log.]
