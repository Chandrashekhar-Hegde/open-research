# Protocol

Date: 2026-09-08. Status: synthetic demonstration, unregistered.

Question: What is the mean within-item change in the eight supplied pairs?
The corpus is exactly `data/raw.csv`; the unit of analysis is one fictional
item. Include all eight rows. No sampling, intervention, randomization, or
population claim is involved. The primary output is mean(after − before), in
arbitrary units. Secondary descriptions are median, range, and sign counts.

Stop after describing all eight valid rows. Reject malformed or duplicate
records rather than silently excluding them. No empirical sample-size
justification, participant consent, or ethics approval is claimed or needed
for this invented fixture. All values were chosen for a readable worked example.

This protocol was authored with the fixture, not preregistered before data
collection. There are no collection events or registered hypotheses.
Any later change to the example's inputs or method must be visible in Git.
